"""
For BFS (Depth First Search) on a graph,
don't forget to use set to track visited nodes and avoid infinite loop.
Iteration is usually simpler to implement for BFS than recursion.
We also need a queue to process the nodes in batch (one batch per depth/level from source node)
"""